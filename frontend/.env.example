# Frontend Environment Variables
# Copy this to .env.local for local dev, or add to Vercel dashboard for production

# Backend API URL — for local dev, leave as-is
# For production, set to your Render backend URL:
# VITE_API_URL=https://closekart-backend.onrender.com/api
VITE_API_URL=http://localhost:3001/api
