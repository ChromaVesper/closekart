# Backend Production Environment Variables
# This file should be used when deploying to Render or other production platforms
# Replace placeholder values with actual production values

# Server Port (Render will set PORT automatically)
PORT=8000

# MongoDB Atlas Connection String (Production Database)
# Format: mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority
MONGO_URI=mongodb+srv://closekart:%23closekart74@cluster0.wy3rb6d.mongodb.net/closekart?retryWrites=true&w=majority

# JWT Secret (use a strong, random string in production)
JWT_SECRET=your_secure_jwt_secret_key_generate_random_string_here

# Node Environment
NODE_ENV=production

# Frontend URL (for CORS configuration)
# Update this to your actual frontend URL when deployed
FRONTEND_URL=https://closekart.netlify.app

# Optional: Enable debug logging
DEBUG=false
