# Frontend Production Environment Variables
# This file is used during the production build process (npm run build)

# Production Backend API URL
# Replace with your actual production backend URL
VITE_API_URL=https://closekart-backend.onrender.com/api

# Optional: Mappls API Key (already in production CDN, no need to expose here)
# VITE_MAPPLS_API_KEY is loaded directly from CDN in MapComponent.jsx
